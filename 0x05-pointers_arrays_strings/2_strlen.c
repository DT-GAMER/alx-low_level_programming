#include "main.h"
#include <stdio.h>

/**
 * strlen_int - A function that prints the length of a string
 * @s: String to find length
 * Return: void
 */

void _strlen(int *s)
{
int n = 0;

while (*(s + n))
n++;
return (i);
}
