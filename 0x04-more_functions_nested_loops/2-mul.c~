#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply two integers
 * @a: First integer to be executed
 * @b: Second integer to be executed
 * Description - Write a function that multiply two integers
 */

int mul(int a, int b)
{
return (a * b);
}
